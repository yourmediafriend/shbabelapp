import appModule, {
  setCurrentBreakPoint,
  setUpPage,
  setIconNavConfig,
} from './reducers/app';

// Export Reducers
export default appModule;

// Export Actions
export {
  setUpPage,
  setCurrentBreakPoint,
  setIconNavConfig,
};