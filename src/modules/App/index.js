import appModule, {
  setCurrentBreakPoint,
  setUpPage,
  setIconNavPosition,
} from './reducers/app';

// Export Reducers
export default appModule;

// Export Actions
export {
  setUpPage,
  setCurrentBreakPoint,
  setIconNavPosition,
};